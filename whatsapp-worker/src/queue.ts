// Poll messages_a_envoyer (statut=en_attente), poste dans le groupe WhatsApp, marque comme envoyé.
export async function pollAndSend() {
  throw new Error("TODO: polling Supabase + envoi Baileys + marquage statut=envoye");
}
